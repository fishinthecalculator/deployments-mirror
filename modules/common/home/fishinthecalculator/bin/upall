#!/usr/bin/env bash

set -eu

declare -a commands=("guix pull" "cd ~/guix-home && guix home reconfigure -L . fishinthecalculator/home-configuration.scm" "nix-channel --update")

for command in "${commands[@]}"; do
    bash -l -c "$command"
done
