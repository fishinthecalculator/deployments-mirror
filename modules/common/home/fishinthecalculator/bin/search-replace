#!/usr/bin/env bash

set -eu

[ "$#" -ne 2 ] && echo -e "Usage: $(basename "$0") A B\n\nSearches for A recursively in the current directory and replaces it with B." >&2 && exit 1

find \( -type d -name .git -prune \) -o -type f -print0 | xargs -0 sed -i "s/${1}/${2}/g"
