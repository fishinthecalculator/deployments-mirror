#!/usr/bin/env bash

set -ex

if [ -z "${HOME_RECONFIGURE_EXPRESSION}" ]; then
    HOME_RECONFIGURE_EXPRESSION="(@ (fishinthecalculator common home ${USER} home-configuration) ${USER}-home-environment)"
fi

set -u

guix home reconfigure -e "${HOME_RECONFIGURE_EXPRESSION}" "$@"
