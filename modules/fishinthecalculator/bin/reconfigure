#!/bin/sh

[ "$(id -u)" -ne 0 ] && echo "ERROR: This script must be run as root!" && exit 1

set -eux

guix_host="$(hostname)"

guix system reconfigure -e "(@ (fishinthecalculator ${guix_host} system config) ${guix_host}-system)" "$@"
